{
  "name": "tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "cypress run --component",
    "open": "cypress open"
  },
  "dependencies": {
    "@braks/vue-flow": "workspace:*"
  },
  "devDependencies": {
    "cypress": "^10.9.0"
  }
}
